运行前需 `pip install requests`（脚本会在 ImportError 时自动 pip install，但建议预先安装）；设置 `GITHUB_TOKEN` 可将速率限制从 60 req/h 提升至 5000 req/h。常用命令：
- `python scripts/scrape.py`：全量抓取并覆盖 data/repos.json
- `python scripts/update_monthly.py --month 6 --report`：抓取 2026-06 新增仓库并生成月度报告