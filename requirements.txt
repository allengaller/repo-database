# Runtime + test dependencies for repo-database.
# For fully-reproducible installs use `uv sync` together with uv.lock,
# which pins every transitive dependency (urllib3, charset-normalizer, ...).
# `pip install -r requirements.txt` still works as a fallback.
requests>=2.28,<3
pytest>=7.0
ruff>=0.4
responses>=0.25
